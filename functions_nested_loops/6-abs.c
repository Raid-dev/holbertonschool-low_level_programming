#include "main.h"
#include <ctype.h>

/**
 * _abs - Computes the absolute value of the integer n
 * @n: The input integer
 *
 * Return: Always int (Success)
 */

int _abs(int n)
{	
	return (abs(n));
}
